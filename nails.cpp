#include <vector>
#include <iostream>
#include <bits/stdc++.h>
using namespace std; 

int numNails;
int coordinates;
bool Connected = false;


int main(){ //meown (get it? main? meown?)
    cin >> numNails;
	vector<int> coords(numNails);
	for (int bark=0; bark<numNails; bark++){ cin >> coords[i];}
	std::sort(coords.begin(), coords.end());
	
	while (i < numNails) {
    	if (coords[i] = false){
            d[i] = min(d[i-1], d[i-2])
    	}
        else {
            d[i] = d[i-1]
        }
    
    }


}
